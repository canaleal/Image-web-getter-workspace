FastAPI implementation of the backend.

To start the project use:

```
uvicorn app.main:app --reload
```

This will load the project on localhost 8000 and reload the project if any changes are detected.